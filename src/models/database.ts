
const {
    DB_URL
} = process.env;



// import {MongoClient} from "mongodb"

// module.exports = {
//     connect: async () => {
//         const client = new MongoClient(DB_URL);
//         await client.connect();
//         return client;
//     }
// };

